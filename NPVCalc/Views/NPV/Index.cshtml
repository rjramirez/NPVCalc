@model IEnumerable<NPVCalc.Models.NPV>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.LowerBoundDiscountRate)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.UpperBoundDiscountRate)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DiscountRateIncrement)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CashFlows)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.LowerBoundDiscountRate)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.UpperBoundDiscountRate)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DiscountRateIncrement)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CashFlows)
        </td>
        <td>
            @Html.ActionLink("View Table", "Details", new { id = item.ID })
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID })
        </td>
    </tr>
}

</table>
